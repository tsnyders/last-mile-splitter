/**
 * Browser Supabase client (for Client Components and Realtime).
 * For server-side code, use: import { createClient } from "@/lib/supabase/server"
 */
export { createClient } from "./supabase/client";
